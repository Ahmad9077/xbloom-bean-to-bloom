# Build-time base path for GitHub Pages deployment (set by CI, not here)
# Example: /xbloom-recipe-worker/
# VITE_BASE_PATH=/your-repo-name/

# The app uses the same-origin backend API (/api/*) — no external URL needed.
# All API requests use credentials: "include" with relative paths.
